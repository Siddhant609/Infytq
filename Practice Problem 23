#PF-Prac-23
def divisible_by_sum(number):
    temp = number
    sum1 = 0
    while temp > 0:
        sum1 += temp%10
        temp = temp//10
    if number%sum1 == 0:
        return True
    else:
        return False

    
number=66
print(divisible_by_sum(number))
